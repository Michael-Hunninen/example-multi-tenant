'use client';

// This file is intentionally named page.client.js to ensure Next.js generates
// the proper client reference manifest during build

export default function FrontendClientComponent() {
  return null;
}
